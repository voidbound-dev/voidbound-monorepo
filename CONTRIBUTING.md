# Участие в разработке Voidbound Chronoscape

Благодарим вас за интерес к разработке! Этот проект следует строгим инженерным практикам для обеспечения долгосрочной поддерживаемости.

## 🏛 Принципы разработки

### 1. Clean Architecture
Код должен быть разделен на независимые слои:
- **Domain:** Чистые бизнес-правила (Entities, Value Objects, Domain Services). Никаких зависимостей от внешних библиотек.
- **Application:** Сценарии использования (Use Cases).
- **Infrastructure:** Реализация репозиториев, работа с БД, сетевые вызовы.
- **Presentation:** UI (React/Babylon.js) или API Controllers.

### 2. Domain-Driven Design (DDD)
- Используйте повсеместный язык (Ubiquitous Language).
- Логика должна находиться внутри доменных моделей, а не в сервисах (избегайте Anemic Domain Model).

### 3. SOLID & Clean Code
- Каждая функция/класс выполняет одну задачу.
- Код должен быть самодокументируемым.

### 4. TDD (Test-Driven Development)
- **Обязательно:** Пишите тесты перед реализацией функционала.
- Unit-тесты для Domain-логики.
- Integration-тесты для Infrastructure-слоя.

## 🛠 Процесс разработки (Git Flow)

1. Создайте ветку из `main` (или `develop`, если проект вырастет).
   - Формат: `feature/short-description` или `fix/issue-id`.
2. Реализуйте изменения, следуя TDD.
3. Убедитесь, что все тесты проходят.
4. Создайте Pull Request.

## 📝 Стандарты оформления

- **TypeScript:** Строгая типизация (`noImplicitAny: true`).
- **Именование:** `PascalCase` для классов/компонентов, `camelCase` для переменных/функций.
- **Коммиты:** Используйте [Conventional Commits](https://www.conventionalcommits.org/) (например, `feat: add skill system logic`).
